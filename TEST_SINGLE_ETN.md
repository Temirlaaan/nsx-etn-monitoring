# 🧪 Тестирование конкретного ETN (10.11.35.122)

## Вариант 1: Быстрый тест (рекомендуется)

Используйте упрощенный скрипт `test_simple.py` - он требует только 2 зависимости.

### Установка зависимостей:
```bash
pip install asyncssh python-dotenv
```

### Запуск теста:

**С .env файлом:**
```bash
# 1. Создайте .env файл
cp .env.example .env

# 2. Добавьте SSH креды в .env:
# ETN_SSH_USERNAME=admin
# ETN_SSH_PASSWORD=ваш_пароль

# 3. Запустите тест
python test_simple.py 10.11.35.122
```

**Без .env файла (креды в командной строке):**
```bash
python test_simple.py 10.11.35.122 admin ваш_пароль
```

---

## Вариант 2: Полный тест через основной проект

Использует весь функционал из основного проекта.

### Установка всех зависимостей:
```bash
pip install -r requirements.txt
```

### Запуск:
```bash
# С настроенным .env файлом
python test_single_etn.py 10.11.35.122

# Или без параметра (по умолчанию использует 10.11.35.122)
python test_single_etn.py
```

---

## Вариант 3: Ручной тест через SSH

Если Python скрипты не работают, проверьте вручную:

```bash
# 1. Подключитесь по SSH
ssh admin@10.11.35.122

# 2. Проверьте сертификат
openssl x509 -enddate -noout -in /etc/vmware/nsx/host-cert.pem

# Вывод будет примерно таким:
# notAfter=Dec 31 23:59:59 2025 GMT
```

---

## Что проверяет тест?

✅ SSH подключение к хосту  
✅ Выполнение команды openssl  
✅ Чтение файла /etc/vmware/nsx/host-cert.pem  
✅ Парсинг даты истечения сертификата  
✅ Расчет оставшихся дней  

---

## Пример успешного вывода:

```
🧪 Тест SSH подключения к ETN

======================================================================
🔍 Тестирование ETN: 10.11.35.122
======================================================================

Параметры подключения:
  Host: 10.11.35.122
  Username: admin
  Password: ********
  Port: 22

🔌 Подключение по SSH...
✅ SSH подключение установлено!

📜 Выполнение команды: openssl x509 -enddate -noout -in /etc/vmware/nsx/host-cert.pem

✅ Команда выполнена успешно!
Вывод: notAfter=Dec 31 23:59:59 2025 GMT

======================================================================
📊 РЕЗУЛЬТАТЫ
======================================================================

📅 Дата истечения: 2025-12-31 23:59:59 UTC
⏰ Дней до истечения: 423

🟢 OK: Сертификат действителен ещё 423 дней

======================================================================

✅ Тест успешно завершен!
```

---

## Возможные ошибки и решения

### ❌ Ошибка SSH подключения

**Причины:**
- Неверный пароль
- Хост недоступен
- Закрыт SSH порт
- Проблемы с сетью

**Решение:**
```bash
# Проверьте доступность хоста
ping 10.11.35.122

# Попробуйте подключиться вручную
ssh admin@10.11.35.122

# Проверьте SSH порт
telnet 10.11.35.122 22
```

### ❌ Файл сертификата не найден

**Причины:**
- Неверный путь к сертификату
- Нет прав на чтение

**Решение:**
```bash
# После SSH подключения проверьте:
ls -la /etc/vmware/nsx/host-cert.pem
cat /etc/vmware/nsx/host-cert.pem
```

### ❌ Таймаут подключения

**Причины:**
- Хост выключен
- Проблемы с сетью
- Firewall блокирует

**Решение:**
```bash
# Проверьте маршрут
traceroute 10.11.35.122

# Проверьте доступность
ping 10.11.35.122
```

---

## Дополнительная информация

Файл сертификата на ETN:
- **Путь:** `/etc/vmware/nsx/host-cert.pem`
- **Формат:** PEM
- **Проверка:** `openssl x509 -text -in /etc/vmware/nsx/host-cert.pem`

Скрипт использует те же методы, что и основной сервис, поэтому если тест прошел успешно - полный мониторинг тоже будет работать.

---

## Следующие шаги

После успешного теста:

1. ✅ Убедитесь, что `.env` настроен правильно
2. ✅ Запустите полный `test_connection.py` для проверки NSX API
3. ✅ Запустите основной сервис: `docker-compose up -d`
4. ✅ Откройте веб-интерфейс: http://localhost:8000

---

**Готово! Теперь можно протестировать конкретный ETN хост! 🚀**
