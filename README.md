# 🔒 ETN Certificate Monitor

Сервис для мониторинга SSL сертификатов Edge Transport Nodes (ETN) из NSX-T Manager с автоматическими уведомлениями в Telegram.

## 🎯 Возможности

- ✅ Автоматическое обнаружение всех ETN из NSX-T Manager через API
- 🔍 Проверка срока действия SSL сертификатов через SSH
- 📊 Веб-интерфейс с dashboard и статистикой
- 📱 Уведомления в Telegram при истечении сертификатов
- 🔄 Автоматический мониторинг по расписанию
- 📈 История проверок и событий
- 🐳 Docker контейнер для простого развертывания

## 📋 Требования

- Docker и Docker Compose
- Доступ к NSX-T Manager API
- SSH доступ к Edge Transport Nodes
- (Опционально) Telegram Bot Token для уведомлений

## 🚀 Быстрый старт

### 1. Клонирование и настройка

```bash
# Клонировать проект
git clone <repository_url>
cd etn-cert-monitor

# Создать .env файл из примера
cp .env.example .env

# Отредактировать .env файл
nano .env
```

### 2. Настройка .env файла

```env
# NSX-T Manager Configuration
NSX_MANAGER_URL=https://nsx01cast.t-cloud.kz
NSX_USERNAME=admin
NSX_PASSWORD=your_password_here

# ETN SSH Credentials
ETN_SSH_USERNAME=admin
ETN_SSH_PASSWORD=your_etn_password_here
ETN_SSH_PORT=22

# Telegram Configuration (опционально)
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here
TELEGRAM_CHAT_ID=your_chat_id_here

# Scheduler Configuration
# Проверка NSX каждые 2 дня в 02:00
NSX_CHECK_CRON=0 2 */2 * *
# Проверка сертификатов каждую неделю в понедельник в 03:00
CERT_CHECK_CRON=0 3 * * 1

# Certificate Expiry Warning (дней до истечения)
CERT_WARNING_DAYS=30

# Database
DATABASE_URL=sqlite+aiosqlite:////data/etn_monitor.db

# Web Server
WEB_HOST=0.0.0.0
WEB_PORT=8000
```

### 3. Запуск с Docker Compose

```bash
# Создать директорию для базы данных
mkdir -p data

# Запустить сервис
docker-compose up -d

# Проверить логи
docker-compose logs -f

# Остановить сервис
docker-compose down
```

### 4. Доступ к веб-интерфейсу

Откройте в браузере: `http://localhost:8000`

## 📱 Настройка Telegram уведомлений

### 1. Создание бота

1. Найдите [@BotFather](https://t.me/botfather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям и получите токен

### 2. Получение Chat ID

1. Отправьте любое сообщение вашему боту
2. Откройте: `https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getUpdates`
3. Найдите `"chat":{"id":123456789}` в JSON ответе

### 3. Добавление в .env

```env
TELEGRAM_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
TELEGRAM_CHAT_ID=123456789
```

## 🔧 API Endpoints

### Основные эндпоинты

- `GET /` - Веб-интерфейс dashboard
- `GET /api/nodes` - Список всех ETN
- `GET /api/nodes/{node_id}` - Детальная информация о ноде
- `GET /api/nodes/{node_id}/checks` - История проверок сертификата
- `GET /api/events` - История событий (добавление/удаление ETN)
- `GET /api/stats` - Статистика для dashboard
- `GET /health` - Health check

### Примеры использования

```bash
# Получить список всех активных ETN
curl http://localhost:8000/api/nodes

# Получить статистику
curl http://localhost:8000/api/stats

# Получить историю проверок для конкретной ноды
curl http://localhost:8000/api/nodes/{node_id}/checks
```

## ⏰ Расписание проверок

По умолчанию:
- **Синхронизация с NSX**: каждые 2 дня в 02:00
- **Проверка сертификатов**: каждую неделю в понедельник в 03:00
- **Telegram уведомления**: ежедневно в 10:00

Формат cron выражений:
```
┌───────────── минута (0-59)
│ ┌───────────── час (0-23)
│ │ ┌───────────── день месяца (1-31)
│ │ │ ┌───────────── месяц (1-12)
│ │ │ │ ┌───────────── день недели (0-6, 0 = воскресенье)
│ │ │ │ │
0 2 */2 * *  - каждые 2 дня в 02:00
0 3 * * 1    - каждый понедельник в 03:00
```

## 📊 Структура базы данных

SQLite база содержит 4 таблицы:

1. **transport_nodes** - информация о всех ETN
2. **certificate_checks** - история проверок сертификатов
3. **node_events** - журнал событий (добавление/удаление)
4. **telegram_notifications** - история отправленных уведомлений

## 🔍 Логирование

Логи доступны через Docker:

```bash
# Посмотреть логи в реальном времени
docker-compose logs -f

# Последние 100 строк
docker-compose logs --tail=100

# Только ошибки
docker-compose logs | grep ERROR
```

## 🛠️ Разработка

### Локальный запуск без Docker

```bash
# Создать виртуальное окружение
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows

# Установить зависимости
pip install -r requirements.txt

# Запустить приложение
python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

### Структура проекта

```
etn-cert-monitor/
├── app/
│   ├── __init__.py
│   ├── main.py              # FastAPI приложение
│   ├── config.py            # Конфигурация
│   ├── database.py          # SQLAlchemy setup
│   ├── models.py            # БД модели
│   ├── schemas.py           # Pydantic схемы
│   ├── nsx_client.py        # NSX API клиент
│   ├── ssh_checker.py       # SSH проверка сертификатов
│   ├── scheduler.py         # Фоновые задачи
│   └── telegram_notifier.py # Telegram уведомления
├── templates/
│   └── index.html           # Веб интерфейс
├── data/                    # База данных (создается автоматически)
├── Dockerfile
├── docker-compose.yml
├── requirements.txt
├── .env.example
└── README.md
```

## 🔒 Безопасность

⚠️ **Важные замечания:**

1. **Не коммитьте .env файл** в Git
2. **SSL verification отключена** для NSX Manager (как требовалось)
3. **SSH пароли хранятся в открытом виде** в .env
4. Рекомендуется запускать в защищенной сети
5. Для production рассмотрите использование:
   - Secrets management (Vault, Docker Secrets)
   - Шифрование базы данных
   - HTTPS для веб-интерфейса

## 🐛 Troubleshooting

### Проблема: Не могу подключиться к NSX API

```bash
# Проверьте доступность NSX Manager
curl -k https://nsx01cast.t-cloud.kz/api/v1/transport-nodes

# Проверьте креды в .env файле
cat .env | grep NSX_
```

### Проблема: SSH подключение не работает

```bash
# Проверьте SSH доступ вручную
ssh admin@<etn_ip_address>

# Проверьте креды в .env
cat .env | grep ETN_SSH_
```

### Проблема: Telegram уведомления не приходят

```bash
# Проверьте токен и chat_id
cat .env | grep TELEGRAM_

# Проверьте логи
docker-compose logs | grep -i telegram
```

### Проблема: База данных заблокирована

```bash
# Остановить контейнер
docker-compose down

# Удалить базу (потеряете историю!)
rm -rf data/etn_monitor.db

# Запустить заново
docker-compose up -d
```

## 📝 Changelog

### v1.0.0 (2024)
- ✨ Первый релиз
- ✅ Интеграция с NSX-T Manager API
- ✅ SSH проверка сертификатов
- ✅ Веб-интерфейс с dashboard
- ✅ Telegram уведомления
- ✅ Docker контейнеризация

## 📄 Лицензия

MIT License

## 👨‍💻 Автор

Разработано для мониторинга Edge Transport Nodes в NSX-T инфраструктуре.

## 🤝 Поддержка

Для вопросов и проблем создайте Issue в репозитории проекта.
