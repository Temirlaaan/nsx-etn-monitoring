# ⚡ БЫСТРЫЙ СТАРТ - ТЕСТ 4 ETN

## 1️⃣ Подготовка (30 секунд)

```bash
# Распаковать
tar -xzf etn-cert-monitor.tar.gz
cd etn-cert-monitor

# Скопировать тестовый конфиг
cp .env.test4 .env
```

## 2️⃣ Настройка .env (1 минута)

Откройте `.env` и замените:
- `YOUR_NSX_PASSWORD_HERE` → ваш NSX пароль
- `YOUR_SSH_PASSWORD_HERE` → ваш SSH пароль (в кавычках если есть #)

**Уже настроено для 4 ETN:**
```env
ETN_WHITELIST=10.11.35.7,10.11.35.8,10.11.35.2,10.11.35.11
```

## 3️⃣ Запуск (10 секунд)

```bash
mkdir -p data
docker-compose up -d
```

## 4️⃣ Проверка

```bash
# Логи
docker-compose logs -f

# Должны увидеть:
# "ETN Whitelist enabled: ['10.11.35.7', '10.11.35.8', '10.11.35.2', '10.11.35.11']"
# "Filtered 4 Edge Transport Nodes (whitelist applied)"
```

## 5️⃣ Откройте браузер

```
http://localhost:8000
```

**Увидите только 4 ETN в таблице!** ✅

---

## 🔄 После теста - мониторить все 114 ETN

В `.env` измените:
```env
# Закомментировать или оставить пустым
ETN_WHITELIST=
```

Перезапустите:
```bash
docker-compose restart
```

---

**Время запуска: ~2 минуты** ⏱️

Полная документация: **TEST_4ETN.md**
